{
  "name": "controltask",
  "version": "1.0.0",
  "description": "Leads",
  "scripts": {
    "start": "parcel public/index.html --no-source-maps --open",
    "build": "parcel build public/index.html --no-source-maps --no-cache --public-url ."
  },
  "author": "MalevanayaMV",
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-clean-easy": "^1.0.2"
  },
  "dependencies": {
    "swiper": "^6.5.0"
  },
  "parcelCleanPaths": [
    "dist",
    ".cache"
  ]
}
